apiVersion: v1
kind: Service
metadata:
  name: svc-globoticket-services-payment
  namespace: globoticket
  labels:
    version: dev
    product: globoticket-services-payment
spec:
  type: ClusterIP
  selector:
    app: globoticket-services-payment
  ports:
  - port: 5006
    targetPort: 5006
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-globoticket-services-payment
  namespace: globoticket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: globoticket-services-payment
  template:
    metadata:
      labels:
        app: globoticket-services-payment
    spec:
      containers:
      - name: globoticket-services-payment
        image: globoticket.azurecr.io/globoticket-payment:#{tag}#
        imagePullPolicy: Always
        ports:
        - containerPort: 5006
        startupProbe:
          httpGet:
            path: /health/live
            port: 5006
          failureThreshold: 30
          periodSeconds: 2
        livenessProbe:
          httpGet:
            path: /health/live
            port: 5006
          initialDelaySeconds: 15          
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 5006
          initialDelaySeconds: 5
          timeoutSeconds: 2
          periodSeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "64Mi"
          limits:
            cpu: "500m"
            memory: "128Mi"
        envFrom:
        - configMapRef:
            name: globoticket-common-config    
        - secretRef:
            name: globoticket-common-secrets