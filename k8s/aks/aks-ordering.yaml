apiVersion: v1
kind: Service
metadata:
  name: svc-globoticket-services-ordering
  namespace: globoticket
  labels:
    version: dev
    product: globoticket-services-ordering
spec:
  type: ClusterIP
  selector:
    app: globoticket-services-ordering
  ports:
  - port: 5005
    targetPort: 5005
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dep-globoticket-services-ordering
  namespace: globoticket
spec:
  replicas: 1
  selector:
    matchLabels:
      app: globoticket-services-ordering
  template:
    metadata:
      labels:
        app: globoticket-services-ordering
    spec:
      containers:
      - name: globoticket-services-ordering
        image: globoticket.azurecr.io/globoticket-ordering:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 5005
        startupProbe:
          httpGet:
            path: /health/live
            port: 5005
          failureThreshold: 30
          periodSeconds: 2
        livenessProbe:
          httpGet:
            path: /health/live
            port: 5005
          initialDelaySeconds: 15
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 5005
          initialDelaySeconds: 5
          timeoutSeconds: 3
          periodSeconds: 5
        resources:
          requests:
            cpu: "100m"
            memory: "64Mi"
          limits:
            cpu: "500m"
            memory: "128Mi"
        envFrom:
        - configMapRef:
            name: globoticket-common-config
        - secretRef:
            name: globoticket-common-secrets
        - secretRef:
            name: globoticket-ordering-secrets